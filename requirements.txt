# Day 1 deliberately uses Python's standard library only. The production LLM is
# invoked through a locally installed llama.cpp binary, never a hosted API.
pypdf>=4.2
chromadb>=0.5,<0.7
sentence-transformers>=2.7
requests>=2.31
pytest>=8.0